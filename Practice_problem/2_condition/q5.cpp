//wAP to find the difference between ASCII of two characters ,take them as input 
#include <iostream> 
using namespace std; 
int main() 
{ 
    char ch ,dh;
    cin>>ch>>dh;
    cout<<"difference b/w two charecter : "<<abs(int(ch)-int(dh));
}